version: "3.8"

services:
  db:
    image: mysql:8.0
    container_name: podcast_mysql
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: podcast_db
      MYSQL_USER: podcast_user
      MYSQL_PASSWORD: 123456789
    ports:
      - "5306:3306"
    volumes:
      - podcast_db_data:/var/lib/mysql
    networks:
      - podcast_net

  app:
    container_name: podcast_server
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8080"
    depends_on:
      - db
    environment:
      DOCKER_ENV: "true"
      DB_HOST: trolley.proxy.rlwy.net
      DB_PORT: 53938
      DB_USER: root
      DB_PASSWORD: JmvcmOdpbKIXqZAbBhzPkfWGLqHeospE
      DB_NAME: railway
      JWT_SECRET: super_secret_123456
      SUPABASE_URL: https://iyqehfbrqwwofajeyudh.supabase.co
      SUPABASE_KEY: ""

      GEMINI_API_KEY: ""
      API_BASE_URL: https://podcastapi.onrender.com
      GOOGLE_APPLICATION_CREDENTIALS: /root/credentials/google-credentials.json
    networks:
      - podcast_net

volumes:
  podcast_db_data: {}

networks:
  podcast_net:
